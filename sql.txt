CREATE TABLE `quiz_state` (
user_id Uint64, 
question_index Uint64,
PRIMARY KEY (`user_id`)
);

CREATE TABLE `questions` (
    id Uint64,
    question Utf8,
    options Utf8,
    correct_option Uint64,
    PRIMARY KEY (`id`)
);

REPLACE INTO `questions` (id, question, options, correct_option)
VALUES
    (0, "Что такое Python?", "Язык программирования;Тип данных;Музыкальный инструмент;Змея на английском", 0),
    (1, "Какой тип данных используется для хранения целых чисел?", "int;float;str;natural", 0),
    (2, "Какой тип данных используется для хранения чисел с плавающей точкой?", "int;float;str;natural", 1),
    (3, "Что выведет следующий код?\nx = 5\ny = 10\nprint(x + y * 2)", "20;25;30;Ошибка", 1),
    (4, "Какой тип данных возвращает функция input() в Python?", "int;float;str;bool", 2),
    (5, "Какой оператор используется для возведения числа в степень в Python?", "^;*;**;//", 2),
    (6, "Как создать список в Python?", "list = {1, 2, 3};list = [1, 2, 3];list = (1, 2, 3);list = <1, 2, 3>", 1),
    (7, "Какой метод списка добавляет элемент в его конец?", "insert();append();add();push()", 1),
    (8, "Какой метод используется для преобразования строки в целое число?", "int();float();str();chr()", 0),
    (9, "Что такое None в Python?", "Специальный тип данных, который обозначает пустое значение;Пустой список;Неопределенная переменная;Логическое значение", 0)
;

COMMIT;